version: '3.8'

services:
  database:
    image: mysql
    container_name: giftapi-mysql

    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: giftapi

    ports:
      - "3306:3306"

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: